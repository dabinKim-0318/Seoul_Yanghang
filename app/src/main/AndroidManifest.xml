<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seoulyanghang.android">

    <application
        android:name=".SeoulYanghangApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeoulYanghang">
        <activity
            android:name=".ui.home.view.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.MainActivityffffff"
            android:exported="false" />
        <activity
            android:name=".ui.user.login.view.LogInActivity"
            android:exported="true" />
        <activity
            android:name=".ui.user.signup.view.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>